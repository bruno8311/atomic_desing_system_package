name: atomic_desing_system_package
description: "Sistema de diseño para Flutter basado en Atomic Design, con componentes reutilizables y plantillas listas para usar."
version: 0.0.5
homepage: https://github.com/bruno8311/fake_store_api_package
environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
